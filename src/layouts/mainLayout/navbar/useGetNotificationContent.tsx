// import mockedContacts from "__mocks__/mockedContacts.json";
// import getNotification from "utils/getNotificaton";

export default function useGetNotificationContent() {
  //   const numberOfAllContacts: number = mockedContacts.length;
  //   const numberOfFavoriteContacts: number = mockedContacts.filter(
  //     (contact) => contact.isFavorite === true
  //   ).length;
  //   const allContacts = getNotification(numberOfAllContacts);
  //   const suggestionNotification = getNotification(numberOfFavoriteContacts);
  //   return [allContacts, suggestionNotification];
}
